import { Injectable } from "@angular/core";
import { of } from "rxjs";
import { toArray } from 'rxjs/operators';


@Injectable()
export class UtilitiesService {
  range(start: number, end: number): number[] {
  if (from > to)
    throw new Error(
      'The second argument value should be greater than the first value'
  }
  
}

